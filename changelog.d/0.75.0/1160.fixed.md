- **LoRA target resolution in `trainer/pretrain.py` and remaining MoE trainers pinned through real trainer setup (#1157 by @Guts1005 in #1160).**
  The unpinned LoRA target resolver call in `pretrain.py:401` is now exercised directly through `PretrainTrainerWrapper._setup_transformers`, ensuring that an unmapped MoE model without `moe_lora` is refused by name rather than surviving. The real trainer harness now parametrises across all seven MoE-wired trainers in Soup (`dpo`, `grpo`, `kto`, `orpo`, `pretrain`, `sft`, and `simpo`).
